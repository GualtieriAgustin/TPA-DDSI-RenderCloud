<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Administración - MetaMapa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f6f9;
        }
        .sidebar {
            background-color: #212529;
            color: white;
            min-height: 100vh;
            padding: 20px;
        }
        .sidebar a {
            color: #ccc;
            text-decoration: none;
            display: block;
            margin-bottom: 10px;
        }
        .sidebar a:hover {
            color: white;
        }
        .content {
            padding: 30px;
        }
    </style>
</head>

<body>
<div class="container-fluid">
    <div class="row">

        {{! Sidebar izquierda }}
        <div class="col-md-3 col-lg-2 sidebar">
            <h4>Admin Panel</h4>
            <hr>
            <a href="/user/admin/">🏠 Home</a>
            <a href="/user/admin/colecciones">📚 Colecciones</a>
            <a href="/user/admin/fuentes">🌐 Fuentes</a>
            <a href="/user/admin/criterios">⚙️ Criterios de consenso</a>
            <a href="/user/admin/solicitudes">🗑️ Solicitudes de eliminación</a>
            <a href="/user/admin/estadisticas">📊 Mostrar estadísticas</a>
            <a href="/">⬅️ Volver al mapa</a>
        </div>

        {{! Contenido principal }}
        <div class="col-md-9 col-lg-10 content">

            <hr id="estadisticas">
            <h4>📊 Estadísticas del Sistema</h4>

            <form method="post" action="/user/admin/obtenerEstadisticas" class="mb-4">

                <div class="mb-3 d-flex gap-3">
                    <div class="flex-fill">
                        <label for="fechaInicio" class="form-label">Fecha de inicio</label>
                        <input type="datetime-local" id="fechaInicio" name="fechaInicio" class="form-control" required>
                    </div>

                    <div class="flex-fill">
                        <label for="fechaFin" class="form-label">Fecha de fin</label>
                        <input type="datetime-local" id="fechaFin" name="fechaFin" class="form-control" required>
                    </div>
                </div>
                <input type="hidden" name="panel" value="true">

                <button type="submit" class="btn btn-primary">Generar Estadísticas</button>
            </form>

            {{#if estadisticas}}

                <!-- Tarjetas resumen -->
                <div class="row mb-4">
                    {{#each estadisticas}}
                        <div class="col-md-4 mb-3">
                            <div class="card shadow-sm border-0">
                                <div class="card-body">
                                    <h6 class="text-muted">{{this.nombre}}</h6>
                                    <h3>{{this.resultado}}</h3>
                                </div>
                            </div>
                        </div>
                    {{/each}}
                </div>

                <!-- Tabla completa -->
                <div class="card shadow-sm border-0">
                    <div class="card-header bg-light">
                        <strong>Detalles completos</strong>
                    </div>
                    <div class="card-body p-0">
                        <table class="table table-bordered m-0">
                            <thead class="table-light">
                            <tr>
                                <th>Consulta</th>
                                <th>Resultado</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#each estadisticas}}
                                <tr>
                                    <td>{{this.nombre}}</td>
                                    <td>{{this.resultado}}</td>
                                </tr>
                            {{/each}}
                            </tbody>
                        </table>
                    </div>
                </div>

                <!-- Descarga CSV -->
                <div class="mt-3">
                    <a href="/user/admin/guardarEstadisticas" class="btn btn-success">
                        📥 Descargar CSV
                    </a>
                </div>

            {{else}}
                <p class="text-muted">No se han generado estadísticas aún.</p>
            {{/if}}

        </div>
    </div>
</div>
</body>
</html>