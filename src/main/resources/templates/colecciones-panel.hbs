{{! Panel de administración }}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Panel de Administración - MetaMapa</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f4f6f9;
        }
        .sidebar {
            background-color: #212529;
            color: white;
            min-height: 100vh;
            padding: 20px;
        }
        .sidebar a {
            color: #ccc;
            text-decoration: none;
            display: block;
            margin-bottom: 10px;
        }
        .sidebar a:hover {
            color: white;
        }
        .content {
            padding: 30px;
        }
    </style>
</head>
<body>
<div class="container-fluid">
    <div class="row">
        {{! Sidebar izquierda }}
        <div class="col-md-3 col-lg-2 sidebar">
            <h4>Admin Panel</h4>
            <hr>
            <a href="/user/admin/">🏠 Home</a>
            <a href="#colecciones">📚 Colecciones</a>
            <a href="/user/admin/fuentes">🌐 Fuentes</a>
            <a href="/user/admin/#criterios">⚙️ Criterios de consenso</a>
            <a href="/user/admin/solicitudes">🗑️ Solicitudes de eliminación</a>
            <a href="/user/admin/#estadisticas">📊 Mostrar estadisticas </a>
            <a href="/">⬅️ Volver al mapa</a>
        </div>

        {{! Contenido principal }}
        <div class="col-md-9 col-lg-10 content">
            <h2>Colecciones</h2>

            <form method="get" action="/user/admin/colecciones/formulario">
                <button type="submit" class="btn btn-primary mb-3">Crear Coleccion</button>
            </form>

            <h4>🌐 Colecciones</h4>
            {{#if colecciones}}
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>ID</th>
                        <th>Titulo</th>
                        <th>Descripcion</th>
                        <th>Modo de navegacion</th>
                        <th>Algoritmo de consenso</th>
                        <th>ID Fuente</th>
                        <th>ID Criterio de Pertenencia</th>
                    </tr>
                    </thead>
                    <tbody>
                    {{#each colecciones}}
                        <tr>
                            <td>{{this.id}}</td>
                            <td>{{this.titulo}}</td>
                            <td>{{this.descripcion}}</td>
                            <td>{{this.modoDeNavegacion}}</td>
                            <td>{{this.algoritmoDeConsenso}}</td>
                            <td>{{this.obtenerFuente.id}}</td>
                            <td>{{this.obtenerCriterio.id}}</td>
                        </tr>
                    {{/each}}
                    </tbody>
                </table>
            {{else}}
                <p class="text-muted">No se han generado colecciones aún.</p>
            {{/if}}

        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>